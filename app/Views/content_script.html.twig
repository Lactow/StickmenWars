{% extends "gencontent.html" %}

{% block aside %}
{% include "aside_script.twig" %}
{% endblock %}

{% block content %}
    
    <div class="col-lg-12 col-md-12 col-sm-1é col-xs-12">
        <h3> Editeur de script</h3>
        <div class="row">
            <form>
                <div class="col-lg-10 col-md-10 col-sm-10 col-xs-10">
                    <div class="form-group">
                        {% if script.name !== null %}
                        <input type="text" name="scriptname" value="{{script.name}}" />
                        {% endif %}
                        {% if script.name === null %}
                        <input type="text" name="scriptname" placeholder="Nom du script"/>
                        {% endif %}
                    </div>
                    
                    <div>
                        <textarea col="50" col="50" >
                            {{script.content}}
                        </textarea>
                        
                    </div>
                </div>
                    <input type="submit" class="btn btn-primary" value="Sauvegarder le script" />
            </form>
        </div>
    </div>
    
{% endblock %}{# empty Twig template #}
